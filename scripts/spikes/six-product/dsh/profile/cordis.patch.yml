- id: agent-default-model
  config:
    provider: as-s2-mock
    model: mock
- id: acp
  config:
    provider: as-s2-mock
    model: mock
- insert:
    - id: agent-sessions-s2
      name: '@agent-sessions/dsh-s2-plugin'
