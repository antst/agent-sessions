---
description: Discover Agent Sessions peers, message them, or control a lane
---

Use the `agent_sessions` MCP tools for this request. List peers before routing
an ambiguous name. Preserve the exact structured success or failure result.
Do not substitute CodeBuddy's native teams, jobs, or shell commands when the
Agent Sessions connector is inactive.
