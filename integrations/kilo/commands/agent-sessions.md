---
description: Use the managed Agent Sessions tool for peers and lanes
---

Use the registered `agent_sessions` tool. Native session identity comes from
Kilo's tool context and `shell.env`, not from a model argument. Do not replace
peer `/tui/*` delivery with the server-owned lane prompt route.
