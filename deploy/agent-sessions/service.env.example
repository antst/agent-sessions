# Optional outbound connection to the one trusted Agent Sessions hub.
# AGENT_SESSIONS_HUB=10.2.17.1:7419

# Optional human-readable host suffix. The stable host ID remains the OS
# hostname recorded by the user daemon.
# AGENT_SESSIONS_HOST_NAME=workstation-a
