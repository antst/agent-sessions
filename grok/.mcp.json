{
  "mcpServers": {
    "agent_sessions": {
      "command": "${GROK_PLUGIN_ROOT}/scripts/native-entry",
      "args": [
        "connector",
        "grok",
        "--release-identity",
        "@AGENT_SESSIONS_RELEASE_ID@"
      ]
    }
  }
}
